import { WritingDashboard } from '../blog/WritingDashboard'

/**
 * The writing dashboard route — creates a new post or edits an existing one.
 * Mounts at /blog/write.
 */
export function WritingDashboardRoute() {
  return <WritingDashboard />
}
